interface:
  display_name: "Herdr Peer"
  short_description: "Review edits and recheck authorized fixes with a peer"
  default_prompt: "Use $herdr-peer to request a read-only review of current-task edits, including known non-sensitive gitignored edits, and re-review authorized fixes until both agents agree no actionable findings remain."

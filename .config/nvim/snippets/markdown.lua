return {
  {
    prefix = 'testcase',
    body = [[---
テストケース名: ${1:テストケース名}
---

# テストケース

## 大区分: ${2:大区分名}

### 機能: ${3:機能名}

#### ${4:テストケース名}

**タグ:** `${5:タグ}`

**事前条件:**
- ${6:条件}

**手順:**
1. ${7:ステップ}

**確認事項:**
- [ ] ${0:チェック項目}
]],
    desc = 'テストケースMarkdownテンプレート (*.testcase.md)',
  },
  {
    prefix = 'tc-spec',
    body = [[#### ${1:テストケース名}

**タグ:** `${2:タグ}`

**事前条件:**
- ${3:条件}

**手順:**
1. ${4:ステップ}

**確認事項:**
- [ ] ${0:チェック項目}
]],
    desc = 'テストケース仕様を追加',
  },
  {
    prefix = 'tc-func',
    body = [[### 機能: ${1:機能名}

#### ${2:テストケース名}

**タグ:** `${3:タグ}`

**事前条件:**
- ${4:条件}

**手順:**
1. ${5:ステップ}

**確認事項:**
- [ ] ${0:チェック項目}
]],
    desc = 'テストケース機能を追加',
  },
  {
    prefix = 'tc-cat',
    body = [[## 大区分: ${1:大区分名}

### 機能: ${2:機能名}

#### ${3:テストケース名}

**タグ:** `${4:タグ}`

**事前条件:**
- ${5:条件}

**手順:**
1. ${6:ステップ}

**確認事項:**
- [ ] ${0:チェック項目}
]],
    desc = 'テストケース大区分を追加',
  },
}

[files]
extend-exclude = [
  ".config/tmux/plugins/", # External tmux plugins (submodules)
]

[default]
extend-ignore-identifiers-re = [
  "iterm", # iTerm2 image protocol type in fastfetch config
]
extend-ignore-re = [
  "sha256-.+", # Nix SRI hashes
]
